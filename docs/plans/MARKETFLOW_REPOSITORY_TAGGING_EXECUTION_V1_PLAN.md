# MarketFlow Repository Tagging Execution v1 Plan

## Purpose

Create exactly the four locally approved annotated governance tags for the terminal Expectancy Lab archive evidence. Preserve tag creation as a local repository operation distinct from remote publication, merge, cleanup, main, predictive, profitability, runtime, or trading authority.

## Source Approval

Bind the committed `MARKETFLOW_REPOSITORY_TAGGING_RELEASE_STRATEGY_APPROVED` artifact at commit `0c7e49ed839543109c39337f67038c8293f9e24f` and digest `7955296dbbd3e218b7d860319707eb98dc15780fad44dcba189a584791e3214a`. Do not rerun the approval, operator review, candidate, inventory, evidence, readiness, archive, or metric chains.

## Repository Context

Start from the clean approval branch. Preserve `origin/main` at `eda58d9a56656641d4e0c2a80a6e572b6e949fc2` and the source observations through 294 local / 266 remote / 560 total branch refs. Verify the four source branches and target commits, the initial 28/0 tag counts, and tag-name absence before mutation.

## Execution Scope

Use `REPOSITORY_TAGGING_EXECUTION_ONLY_LOCAL_TAGS_NOT_PUSH_NOT_MERGE_NOT_DELETE_NOT_MAIN`. Only `git tag -a` for an approved name, approved target, and fully bound message is a permitted Git mutation.

## Approved Terminal Tags and Local Creation Method

Preflight all four names and commits before creating any tag. Existing annotated tags are accepted only when their target and message exactly match; mismatched, lightweight, or unverifiable tags block execution without overwrite or deletion. Create missing tags with argument-list subprocess calls, fixed execution timestamp, exact message, and no shell interpolation. Verify the resulting tag object type, peeled commit, object SHA, and full message.

The approved package contains the final-archive-not-ready, archive-record-not-ready, operator-selection-option-a, and readiness-not-ready tags only.

## Tag Message Template

Each message identifies the exact artifact, status, decision, and source digest. It explicitly says research/governance-only, predictive usefulness and profitability `NOT_ACCEPTED`, runtime and trading/broker use `NOT_AUTHORIZED`, and no trade recommendation created.

## Tag Count Summary

Record total and candidate-namespace counts before and after execution, plus newly created and existing matching counts. The expected first-run transition is 28/0 to 32/4, with four created and zero pre-existing matches. Record actual observations when they differ.

## Next Chain and Gates

The next task is `MARKETFLOW_REPOSITORY_TAGGING_EXECUTION_RESULTS_REVIEW_V1`. Any remote tag publication requires a separate tag-push strategy candidate. Merge planning follows the execution review and tag-push decision. Cleanup planning, approval, execution, and protected main push remain separate later gates.

## Risk Controls

Enforce all 35 controls: only four approved local annotated tags; no tag push, merge, rebase, deletion, main push, force-push, prune, or `origin/main`/`.marketflow` modification; no provider, data, source rerun, metric, model, scoring, recommendation, predictive, profitability, runtime, or broker action; exact names and commits; mismatches block; separate review and push strategy; preserve terminal evidence and the META limitation.

## Non-goals and Guardrails

Do not publish tags, create releases, merge or clean branches, inspect `.env`, access providers, acquire or regenerate data, recompute metrics, train models, score strategies, generate recommendations, modify broker code, or create runtime/trading authority. Never overwrite or delete an existing tag. Generated `.marketflow` and packaging artifacts remain untracked.

Tests exercise Git mutations only in pytest temporary repositories or deterministic fixture mode. The real execution uses the repository only for the four explicitly approved local annotated tags. MarketFlow remains research and decision-support software.
