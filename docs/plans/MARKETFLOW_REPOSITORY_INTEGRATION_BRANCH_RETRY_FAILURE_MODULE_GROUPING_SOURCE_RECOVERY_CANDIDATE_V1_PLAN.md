# MarketFlow Repository Integration Branch Retry Failure Module Grouping Source Recovery Candidate v1 Plan

## Purpose

Create an offline, digest-bound candidate describing controlled future methods
to recover or expose module-grouping source detail. This candidate does not
perform recovery or read the detached cache.

## Source Blocked After-v2 Execution

Bind blocked execution digest
`7eb4bb7bd1ed0e0d2a66688f840aa352a335016533ed7f2c1c11b4d019ec4755`
and blocked-manifest digest
`c3d644957eb536ede1d725c912f0211a0d84aa72e56d5f8cbed2e0939a907cef`.
Preserve `MODULE_GROUPING_DETAIL_NOT_AVAILABLE_FROM_COMMITTED_SOURCE_ARTIFACTS`
as the reason that prioritized planning did not run.

## Source Classification Results Review v2

Bind results-review digest
`0e3e8eb524ec199e0a02de6837900c344fcc72a596d2468663b79ad0f9571e86`,
execution-v2 digest
`054d1d38269731c529637e815086c22c9087e8f018a2456b62cc3b23062ce017`,
and module-grouping digest
`34dbe78cb299882e87aa12a226b1a356a32e98799040c826d7cab53dfbb0baff`.

## Retry Failure Context

Preserve the first retry as authoritative: `24,877 passed / 1,292 failed / 112
errors / 7 skipped`, commit
`ab178b65c69f0274b0abbf9c20df102d35e78d34`. The prior root regression is not
retry evidence and must not replace or revise those counts.

## Known Available and Missing Detail

Available: aggregate retry counts, 1,404 failed-or-errored node IDs, 29 modules,
top aggregate counts `136, 131, 122, 112, 111`, the module-grouping digest, and
source digests.

Missing: module paths, per-path counts, bounded per-module samples, grouping
report content, and a committed snapshot suitable for planning re-entry. Never
infer these values from counts, digests, names, or prior intuition.

## Candidate Scope

Create only
`REPOSITORY_INTEGRATION_BRANCH_RETRY_FAILURE_MODULE_GROUPING_SOURCE_RECOVERY_CANDIDATE_ONLY_NOT_APPROVAL_NOT_EXECUTION_NOT_CACHE_READ_NOT_RETRY_NOT_MAIN`.
Set candidate creation/readiness true while keeping selection, approval,
authorization, execution, and every recovered-detail field false.

## Candidate Philosophy

The blocked planning execution established that aggregate evidence is
insufficient. The next safe step is controlled source-method selection that can
recover or expose detail later without inventing identities or rerunning the
failed retry. Candidate output grants no recovery or runtime authority.

## Proposed Module Grouping Source Recovery Packages

Define ten packages:

1. Read the reviewed detached pytest cache read-only in a separately approved execution.
2. Expose a digest-verifiable Classification Execution v2 output if locatable.
3. Accept an explicit operator-provided original report path.
4. Create a bounded committed summary from verified cache under high control.
5. Recover only top module paths from verified cache.
6. Block aggregate-only planning as insufficient.
7. Block inferred module names.
8. Block rerunning the authoritative retry.
9. Block direct remediation or retry without recovered source detail.
10. Block main merge despite missing detail and failed retry.

All packages remain unselected, unapproved, and unexecuted.

## Recommended Package

Recommend
`PACKAGE_RECOVER_MODULE_GROUPING_DETAIL_FROM_REVIEWED_DETACHED_PYTEST_CACHE_READ_ONLY`
for operator review only. It is the proven original grouping source and can
avoid a pytest rerun, but any cache access requires separate approval and must
verify reviewed hashes and counts before parsing.

## Future Source Recovery Requirements

Define all 23 requirements covering source/digest/count binding, unsupported
claims, no retry/full pytest/diagnostic command, no path inference, fail-closed
hash/count/detail checks, ignored runtime directories, protected refs and staged
evidence, results review before planning re-entry, and separate retry/main-merge
gates.

## Future Source Recovery Plan

Define ten `PLANNED_NOT_EXECUTED` steps: bind source digests, verify boundaries,
select one approved source, validate hashes/counts before any cache parse,
recover bounded detail, produce a source artifact, preserve unsupported claims,
require results review, and keep downstream authority closed.

## Planned Outputs

Define ten `PLANNED_NOT_GENERATED` outputs: recovery manifest, grouping detail,
counts by path, bounded samples, top-module detail, cache verification,
unsupported-claims boundary, limitations, planning-reentry readiness, and digest
manifest.

## Non-Goals

Do not recover or expose detail now; access/modify cache; parse logs; run
diagnostics, remediation, classification, retry, or full pytest; create results
or retry candidates; claim unsupported findings; mutate protected state or
evidence; call providers; or grant predictive, profitability, runtime, or
trading authority.

## Next Chain and Gates

Require candidate operator review, conditional approval, conditional execution,
source-recovery results review, and reviewed planning re-entry before any
remediation review, diagnostic-capture candidate, or separately approved retry
chain. Thirteen chain stages and fifteen gates preserve these transitions.

## Risk Controls

Apply all 58 controls protecting the candidate-only boundary, failed-retry
authority, source authenticity, origin/main, the integration branch/worktree,
staged evidence, terminal archives, governance tags, and the META limitation.

## Guardrails

The default path is deterministic and offline. No `.pytest_cache` or
`.marketflow` content is read, modified, staged, or committed. Future recovery
must fail closed on missing detail or any reviewed hash/count mismatch.

## Next Task

`MARKETFLOW_REPOSITORY_INTEGRATION_BRANCH_RETRY_FAILURE_MODULE_GROUPING_SOURCE_RECOVERY_CANDIDATE_OPERATOR_REVIEW_V1`
