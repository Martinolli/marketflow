# MarketFlow Repository Integration Branch Retry Failure Module Grouping Source Recovery Execution v1 Plan

## Purpose

Execute the approved read-only cache package and recover bounded module grouping
detail without rerunning pytest or changing repository/runtime authority.

## Source Approval

Bind approval digest `3b2e00be71e6aa209520bba347397bc12134566adfd30ff29e432ba0c7ce4b76`
and the selected reviewed detached-cache package.

## Source Blocked After-v2 Execution

Preserve the original missing-detail reason and its execution/manifest digests.
Recovery resolves only the committed-detail gap; it does not revise the failed
retry or blocked planning result.

## Source Classification Results Review v2

Bind the reviewed classification digests, 1,404 node IDs, 29 modules, and top
counts `136, 131, 122, 112, 111` as verification expectations.

## Retry Failure Context

The authoritative retry remains 24,877 passed, 1,292 failed, 112 errors, and 7
skipped. Root regression remains non-retry evidence.

## Cache Verification

Read only `lastfailed` and `nodeids`; hash raw bytes, parse JSON, verify expected
counts, require the failed-node set to be a subset of node inventory, and fail
closed on any mismatch.

## Source Recovery Scope

Extract text before the first `::` as module path. Sort modules by descending
count then ascending path. Sort node IDs per module and retain at most five.

## Recovered Module Grouping Success Path

Generate all ten research-only reports and deterministic recovery/detail
manifests only when cache and recovered-summary checks all match.

## Blocked Cache or Source Mismatch Path

Create a deterministic blocked artifact and route to execution failure diagnosis
for missing/unparseable cache, hash/count mismatch, subset failure, or recovered
summary mismatch. Never repair, infer, copy, or rerun pytest.

## Unsupported Claims Boundary

No failure/error separation, first-order claim, traceback root cause, direct
remediation, retry success, or main-merge readiness is supported.

## Authority Boundaries

No planning re-entry, retry candidate, retry, main merge, runtime, or trading
authority is created by recovery execution.

## Next Chain and Gates

On success, results review is first, followed only conditionally by planning
re-entry, diagnostic/remediation governance, a new retry chain, and main-merge
approval. On failure, diagnosis/remediation gates keep planning and main closed.

## Risk Controls and Guardrails

Fifty-four controls preserve read-only cache access, protected refs and evidence,
unsupported claims, separate review/approval gates, and provider/data/model/
runtime/trading closure. Never modify or commit runtime cache directories.

## Next Task if Successful

`MARKETFLOW_REPOSITORY_INTEGRATION_BRANCH_RETRY_FAILURE_MODULE_GROUPING_SOURCE_RECOVERY_RESULTS_REVIEW_V1`

## Next Task if Blocked

`MARKETFLOW_REPOSITORY_INTEGRATION_BRANCH_RETRY_FAILURE_MODULE_GROUPING_SOURCE_RECOVERY_EXECUTION_FAILURE_DIAGNOSIS_V1`
